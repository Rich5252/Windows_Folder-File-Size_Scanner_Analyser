Simple Windows File  Size scanner that scans a given root folder and displays a tree view of the folders with their sizes. Any folder can be clicked to show its list of files in a separate ListView control.
The results are updated dynamically during the scan.

Written in C# using VS2026.
